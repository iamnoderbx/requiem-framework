import { Start, BaseController, Controller } from "shared/controllers/components";

@Controller()
export class Testing extends BaseController implements Start {
    constructor() {
        super()
    }

    start(): void {
        
    }
}